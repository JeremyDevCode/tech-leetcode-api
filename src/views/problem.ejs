<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>
        <%= problem.title %>
    </title>
    <style>
        /* Custom color for example/constraint headers */
        .prose p strong:contains("Example"),
        .prose p strong:contains("Constraints") {
            color: #2563eb;
            /* Tailwind blue-600 */
            font-weight: 600;
        }
    </style>
</head>

<body class="bg-gray-50 min-h-screen p-6">

    <div class="max-w-4xl mx-auto bg-white rounded-xl shadow-lg p-6">

        <div class="flex items-center justify-between mb-4">
            <h1 class="text-3xl font-bold">
                <%= problem.title %>
            </h1>
            <span class="
                px-3 py-1 text-sm font-semibold rounded-full
                <% if (problem.difficulty === 'Easy') { %> bg-green-100 text-green-700 <% } %>
                <% if (problem.difficulty === 'Medium') { %> bg-yellow-100 text-yellow-700 <% } %>
                <% if (problem.difficulty === 'Hard') { %> bg-red-100 text-red-700 <% } %>
            ">
                <%= problem.difficulty %>
            </span>
        </div>

        <div class="flex gap-4 mb-4 text-sm text-gray-600">
            <div>👍 <%= problem.likes %>
            </div>
            <div>👎 <%= problem.dislikes %>
            </div>
            <div>Acceptance Rate: <%= problem.acRate %>
            </div>
        </div>

        <hr class="my-4" />

        <!-- Problem Description -->
        <div class="prose max-w-none
            prose-pre:bg-gray-900 prose-pre:text-green-400 prose-pre:rounded-lg prose-pre:p-4
            prose-code:text-pink-500 prose-code:bg-gray-100 prose-code:px-1 prose-code:rounded">
            <%- problem.content %>
        </div>

        <!-- Hints -->
        <% if (problem.hints && problem.hints.length> 0) { %>
            <div class="mt-10 border-t border-gray-300 pt-6">
                <h2 class="text-lg font-semibold text-gray-800 mb-4">Hints</h2>
                <ul class="list-disc list-inside space-y-1">
                    <% problem.hints.forEach(hint=> { %>
                        <li>
                            <%= hint %>
                        </li>
                        <% }) %>
                </ul>
            </div>
            <% } %>



    </div>

</body>

</html>